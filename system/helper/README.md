# System Helper Directory

This directory is reserved for custom helper functions that can be used across the OpenCart installation.

Currently, the Anima theme does not require custom helper functions.
