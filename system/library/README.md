# System Library Directory

This directory is reserved for custom library classes that can be used across the OpenCart installation.

Currently, the Anima theme does not require custom library classes.
