# JavaScript Directory

This directory is reserved for custom JavaScript files and libraries.

Currently, the theme uses OpenCart's built-in JavaScript libraries and does not require additional custom JavaScript files.

## jQuery DateTimePicker

The jquery/datetimepicker subdirectory structure is included for future use if date/time picker functionality is needed.
